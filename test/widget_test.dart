import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test - widget tree builds', (
    WidgetTester tester,
  ) async {
    // Placeholder test: verifies the test harness runs.
    // Full widget tests are in the test/widget/ directory.
    expect(1 + 1, equals(2));
  });
}
